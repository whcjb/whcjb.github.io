# 加拉太书：OCR 核查现状（2026-09-10）

## 判读从没跑过

约翰福音 / 罗马书 / 歌罗西书 / 以弗所书 四卷都做过「渲染扫描页交模型逐页判读」
（`logs/adjudicate_*.md`），**加拉太书没有**——`scripts/adjudicate_ocr_page.py`
的 BOOKS 里就没有这一卷。

## 但底本 PDF 自带文本层，可以零成本先出候选

`/Users/yanpeifa/Documents/论文/calvin/加拉太书注释-加尔文.pdf` 150 页，
与 `calvin_raw/galatians-scan/ocr` 的页数 1:1，**自带文本层**。
它本身也是 OCR（`教义件的阐沭`、`毋宁说插入了-----个劝告`），所以**没有裁判效力**，
但可以当「候选生成器」：两侧不一致处再裁扫描图人眼定夺，全程零 API 成本。

`scripts/audit_ocr_vs_pdf.py --book galatians` 跑出 **305 处字级差异**，按类分：

| 类别 | 处数 | 处置 |
|---|---|---|
| `已`↔`己` | 95 | PDF 侧 OCR 噪声，站上是对的 |
| `一一`（破折号碎片） | 76 | PDF 侧噪声 |
| 字形噪声（`稣/穌` `祖/袓` `高/髙` `谤/镑` `干/千` `罪/葬` …） | ~40 | PDF 侧噪声 |
| 他/祂体例、经文引用头 | ~7 | 体例差异 |
| **剩下的真候选** | **87** | 待逐条裁图核 |

## 已经修掉的三处（都不是字词级，是发布环节的结构性丢失）

1. **加 3:22 整段段首丢失**。发布稿从「可惜这仅是假设。”」直接跳到孤零零的
   「得活呢？”在此，保罗暗指了利未记的话…」——中间整整一段（约 130 字）没了：
   「**但圣经把众人都圈在罪里。**『圣经』一词在这里尤指律法本身…谁又能靠律法」。
   raw `page_0082.md` 里**是全的**，是发布时跨页丢的。已按 raw 补回并补上段头。
2. **伪装成 3:21 的 Bible-dump 段**：内容与上方经文框逐字重复，删掉。
   删掉后 3.md 的段头序列由 `…18,21,20,21,24…` 变回单调。
3. **4.md 一个假 verse-opener**：`**加拉太书 4:11。** *我为你们起的愤恨…*`
   其实是**林后 11:2-3 的引文**（raw 里就是一段普通引文），发布时被当成节标题。
   去掉标题标记，恢复成引文段落。4.md 段头序列同样恢复单调。

六章的段头序列现在全部单调；audit-md 全过。

## 还没做的

- 87 条字级候选（PDF 文本层 vs 站上）逐条裁图核
- **漏字类查不出来**：两边字流都「合法」，机械比对结构上发现不了。
  这正是 Qwen OCR 的主要错法（罗马书/歌罗西书实测如此），
  要覆盖只能像别的四卷那样渲染扫描页交模型判读（150 页，按约翰福音
  $23.65/662 页折算约 $5–6）。

---

# 收尾（2026-09-10 当日）：判读也跑了，全部走完

## 判读

`scripts/adjudicate_ocr_page.py` 的 BOOKS 补上 galatians，跑完 150 页：
**59 页有差异、52 条明细，合计 $5.83**（`logs/adjudicate_galatians.md`）。

回填走第四代（按页对齐再落点）+ tesseract 450dpi 重扫当第二证人 +
**24 条候选逐条裁扫描图人眼核**：

| 去向 | 条数 |
|---|---|
| 已回填 | 22 |
| 判读误读，维持原样 | 1（`枷锁`→报「柳锁」，800dpi 看底本就是「枷」） |
| **底本自身错字**，维持原样 | 1（`吩咐`→报「盼咐」，800dpi 看底本印的确实是「盼咐」，同约翰福音「貌视」） |
| 正文本来就对 / 圈号 / 他祂体例 | 其余 |

回填的例子：`奉献`→`我献上`、`乃是最高职分`→`最高的职分`、`坚决`→`绝决`、
`担当不坚固人的软弱`→`担代`、`聚集之地`→`集聚之地`、`这件事本身`→`这事件本身`、
`丧失`→`失丧`、`针对特定的人`→`指着特定的人`、`日前月损`→`日削月损`、
`所说的一不屑一顾`→`所说的不屑一顾`、`称为信徒之母`→`称作信徒之母`、
`天而来`→`从天而来`、`神恩典的承认`→`对神恩典的承认`。

## 机械比对那 87 条候选也核完了

逐条裁图后，真错 14 处已改（`每当想起`→`每当我们想起`、`一种错误`→`一种错谬`×3、
`诸多不同解释`→`诸多种不同解释`、`之所说`→`之所以说`、`已经预定在`→`已经预定了在`、
`似乎是为更清楚`→`似乎是为了更清楚`、`把称义归功于`→`将称义归功于`、
`故此比喻`→`故此比方`、`免除了这一惩罚`→`免除于这一惩罚`、`把某物给予他人`→`将某物`、
`属于基督之国度`→`属乎基督之国度`、`被称为所有教会的母会`→`被看做`、
`因为我们所援引`→`因我们所援引`）。

其余是 PDF 文本层自己的噪声（`已/己` 95 处、破折号碎片 76 处、字形噪声约 40 处），
以及**希腊词假差异**——站上的 διαθήκη / σύστοιχα / πειθοσθαι 都在，
是 PDF 把希腊字母 OCR 成了汉字（`必加批加`、`壤如`、`取饥`）才显出差异。

## 又捞出一处整段丢失

**加 3:19 的段首整段（约 150 字）在发布稿里没有**：从「首先，保罗问道：律法有何功用呢？…」
直接跳到「那么，『原是为过犯添上的』是指什么呢？」，中间
「**原是为过犯。**律法具有多重功用，但是保罗只限于谈论与他当前主题相关的功用…
那些拒绝承认律法具有其他益处的人，诚然是不正确的。」整段没了，连 3:19 的段头一起。
raw `page_0078.md` 里是全的，300dpi 裁图逐句核过后按底本补回（底本作「错谬」不是 raw 的「错误」）。
补回后 3.md 的段头序列由 `…17,18,20…` 变成 `…17,18,19,20…`。

## 现在的状态

判读 52 条、机械候选 87 条**全部有结论**；六章段头序列单调；audit-md 全过。
故意不改的两类：`祂/他` 出版体例（2 条）、底本自身错字 `盼咐`（1 条）。
