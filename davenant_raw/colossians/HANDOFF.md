# Davenant 校勘：当前进度与恢复指引

最后更新：2026-09-18。产物**已重建并落库**，规则与产物同步。

## 一句话状态

原书「整体右移排成一块」的版式（分析表的各支／清单／引诗）现在按几何认出来
并成一块渲染，不再是一条一段（见下面「2026-09-18 这一轮」）；残留普查仍是
9 处；跨页脚注合并那个开关**量过了，维持关着**。欠着的还是**第 1–3 章中译
与当前英文对不上**（缓存命中 20%，本来就欠着，与这一轮无关）。

## 2026-09-18 这一轮：缩进块（[OUTLINE] / dv-outline）

用户拿第四章截图问「英文原文就是这种排版吗」——不是。原书 v2p232
`The principal divisions of this Chapter are three:` 底下的
Exhortations / Commendations / Salutations 是一张**整体右移一大格的分析表**，
行距仍是行距；抽取器按「缩进＝新段落」读，一条一段，发布出来是十几个孤零零
的短段。花括号那几处（`brace_blocks.json`）是按影像手工重建的，**没画括号、
只靠缩进**的这一类罩不住。

1. **判据按几何量，不再手工列表**（`extract_davenant.outline_items`）。门槛
   分两档：连续 ≥2 行且左边界互相对齐（±25px）走低门槛
   `max(2.2·em, 1.5·本页段首缩进)`——v1p319 那张「1.–6.」只比段首深 45px，
   高了就漏；孤身一行抬到 `max(3·em, 2·本页段首缩进)`，还要求与本页已认定
   的某条对齐、上一行以 `,` `:` `;` 收尾。全大写的行一律不算（`EXPOSITION`
   / `THE FOURTH CHAPTER.` 是居中章题）。
   排掉的错判都留在注释里：居中小标题（v2p284 `Observations.`、v1p126
   `From the Scriptures.`）、页面歪斜让普通段首量到 116px 的 v2p136。
2. **产物**：几何上认 64 块，其中 51 块被经文对齐那一步吃掉（经文是悬挂缩进，
   续行本来就右移），落到正文的 13 块 / 40 条：四章的分析表 4 处、拉丁与英文
   引诗 8 处、v1p553 经文块的尾巴 1 处。文字**一个词没动**——拿同一套规则数，
   Gate W 的词多重集与改动前完全一致（318,774 词 / 缺 2,162 / 多 1,941）。
3. **顺带修掉一处黏段**：右移块之后正文接着往下排是**不缩进**的（v2p298 两行
   引诗之后 `But now among the many operations…` 顶格续排），只看缩进会把整段
   散文读成诗句的续行。行一级加了一条判据，全书触发 2 处（另一处 v1p86 本来
   就被经文对齐切开了）。
4. **发布**：`<div class="dv-outline" markdown="1">`，条与条之间 `<br />`，
   一块一段落。斜体、脚注配对、`enum_lead` 都逐条走原来那一套，只有版式变了；
   `ref_budget` 一并认 OUTLINE，否则「这一页该有几条注」的物理判据会少数几个符号。
   样式在 `_layouts/davenant-chapter.html` 的 `.dv-outline`，与手工重建的
   `.dv-cites` 各自为政。
5. **中译**：第四章重发（7 块需译，其余 446 块全命中缓存），`<br />` 结构由
   SYSTEM 2c 条约束、`check_block` 数标签兜底。第 1–2 章的中文页还是旧版式，
   等那两章**本来就欠着的**重译一起出（现在重跑 ch1 要译 1,373 块）。
6. **体检脚本** `scripts/qa_davenant_outline.py`（O1 规则与产物同步 / O3 发布页
   结构）。O1 留了 1 处已核定的不一致：v1p569 那首四行诗的第三句，OCR 把页边
   斑点连着读进行首（x0 296→34），几何上就不算右移行。
7. **验收**：逐词回退 0；渲染层 0；花括号表 6 块全在；两次 publish 逐字节相同；
   kramdown 单独试过 `markdown="1"` + `<br />` 的渲染（一个 `<p>`、脚注引用活着、
   `1.` 没被劫成 `ol`）。

## 2026-09-17 第二轮：第四章英文定稿（翻译前的清底）

按用户要求「①花括号表 ②散点 ③注的切分 ④重跑验收，没问题了再翻译」做完。

1. **花括号分析表**：原书用大括号分支排版的 4 处（全在第四章：v2p246/275/
   292/296，另有 p228 一行居中小标题被拆成两段）按 600 dpi 页面影像手工重建，
   落进 `brace_blocks.json`（对不上产物直接报错）。新增 `[BRACE]` tag 一路
   走到发布，layout 配了 `.dv-brace`。体检脚本 `qa_davenant_brace.py`。
2. **第四章散点**：13 条人工票（序号 `l|.`/`]l.`、`a’`、双标点、`Corollaries. )`、
   章题 `or`→`OF`、脚注引导点、Œ 连字 7 处）+ 页眉正则放宽（`TO TIE`/`TO TIIE`
   共 4 行页眉原先整行拼进正文）。
3. **注的切分**：判据换成**正文里的脚注符个数**——原书每条注在正文都留着
   `*`/`†`，数符号就知道该页有几条注。注区内部只排候选界（行首带符号 >
   「首字母大写 + 上一条句末标点收尾」），取到该有的条数为止，页首那条续注
   并回上一页。注 247 → 227，半截注 48 → 10（第四章 5 → 0），句中插号 1 → 0。
   ⚠️ 老的 `MERGE_CROSS_PAGE` 开关已删；它和它的教训记在 collect_notes 里。
4. **验收**：逐词回退 0；词→词 48 处全部是人工核定与脚注重排号；渲染层 0
   （配对检查加了 ul/ol/li）；连跑两次 publish 逐字节相同。

**中译的现状（重要）**：已发布的 1–3 章中文页**本来就和英文对不上了**——
中文块 1479/778/963 对英文 1472/782/966，中文脚注 163/43/48 对英文
130/36/42。拿 `zh_cache` 按当前英文算命中率只有 20%，而**改动前也是 20%**
（改动前 288/152/183，改动后 284/152/183）。也就是说这一轮改动几乎没有增加
重译成本，1–3 章的重译是本来就欠着的。

## 2026-09-15 那一轮

1. 昨天没提交的「15 页编者长注不再落进正文」验收后落库（`67622ff13`）。
2. Gate ⑤ 残留 31 → 9：20 条按位置核定的票（`ts→is/its`、`tt→it`、
   `iu/tn→in`、`bv→by`、`?f→if`、`sAa//→shall`…），全部拿 IA 文本层
   按位置对读过，并先数过该词形在该页只出现一次。
3. 人工票的键新增**后词限定**（`2|474|change.>taking`）——同页同词形出现
   两次时，只按「卷+页+词形」投票会把对的那处一起改坏。
4. `de. serving → deserving`：换行连字符读成句点、两半分处两页，抽取时的
   DOT_SPLIT 碰不到，补在附卷发布的跨页接缝上（全书只此 1 处）。
5. 附卷与索引发布不再每跑一次就把 `date` 刷成当下——原先**连跑两次 publish
   都做不到零差异**。现在两次逐字节相同。
6. **跨页脚注合并（MERGE_CROSS_PAGE）真开了一轮量过，结论是不能开**：
   不合并 247 条注 / 正文脚注符 239 个，两者只差 8，说明全书顶多容得下
   8 条「没有符号的续注」；而以小写起首的注有 48 条。真开：注 247 → 178、
   小写起首 48 → 14，代价是正文多出 34 个配不上的字面星号（58 → 92），
   一比一。再放宽成「同页续行也并」更糟（注 132、星号再 +39）。
   **要治的是注的切分**——每页该有几条注，物理判据是该页正文里的符号个数。
   数字已写进 `publish_davenant_en.py` 的开关声明旁。

## 要再跑一轮时的三步

```bash
# ① 从干净基线重建（约 45 分钟，四段：正文→发布→附卷→索引）
git checkout -- davenant/colossians davenant_raw/colossians/davenant_colossians_{structured,appendix,index}.txt
bash scripts/davenant_rebuild_all.sh
# ② 验收
bash scripts/davenant_verify.sh
# ③ 逐条核 verify 里「词→词改动」那一档，没问题就提交
```

三个工具脚本都已入库，不在 scratchpad 里：

| 脚本 | 做什么 |
|---|---|
| `scripts/davenant_rebuild_all.sh` | 四段重建：正文抽取→发布→附卷→索引 |
| `scripts/davenant_verify.sh` | 五关验收：回退闸 / 结构闸 / 渲染层 / 已识别错误类残留普查 |
| `scripts/davenant_dryrun.py` | **加任何新规则后必跑**：把规则在全书 3.3 万行上试跑，按规则分类列出它想改的全部改动 |

## 这一轮做了什么

按用户要求「按章分段读——读一段修一类」，用眼睛读完了
**第四章、附卷七章、法国之争、六种索引**（前三章是上一轮读的）。

### 新增规则（`scripts/davenant_witness.py`，除非另注）

| 规则 | 修什么 | 量级 |
|---|---|---|
| `quote_repair` | 前导双引号 `"The` → `The`（原书用斜体标引语，正文根本不排双引号） | ~100 |
| `ifit_repair` | `Jf/Jt/?f/[t/[f/Js/?s/[s/Jn//f//t/\|f/\|t/!f` → If/It/Is/is/In | ~40 |
| `iglyph_repair` | 孤立大写 `I` 被读成 `l \| ] [ 1 4 J L / Y i`，靠**闭合动词表**定位 | ~25 |
| `one_repair` | 书卷序号位 `] Cor.` → `1 Cor.` | 7 |
| `romanref_repair` | 引文位罗马数字 `in./ill./i1./iw./ti./vill.` | 5 |
| `fndot_repair` | 功能词后多余句点 `power of. the` | ~35 |
| `fw_repair` | 短功能词读花 `ts→is` `aud→and` `bv→by` | ~570 |
| `letter_repair` | 词内单字母字形 `servaut→servant`（≥6 字母） | ~200 |
| `apos_head_repair` | `T'he` → `The` | 少量 |
| `glue_repair` 扩充 | `A//`→`All`、`£o/(o`→`to` | 13 |
| 逐位粘连 | 证人在该处拆成两词且我方不是词 → 采信 | ~15 |
| `STRAY_PUNCT` 扩充 | 加 `: ! ) ° · ' '` | ~25 |
| 书眉剥离（提取器） | `250 AN EXPOSITION OF $T. PAUL'S Chap. iv.` 嵌在段落中缝 | 19 |
| `HYPHEN_SPLIT`（提取器） | `ef- fect` → `effect` | 11 |
| `DOT_SPLIT`（提取器） | 换行连字符被读成句点 `Chris. tian` → `Christian` | 26 |
| 点线残渣（索引） | 硬/软两档，软档单独出现时不收（那多半是卷号） | ~40 |
| `fix_vol`（索引） | 卷号 `1I./IT./Il./LI.` → `II.`，按**笔画数**还原 | ~48 |
| `qa_davenant_regress.py` | 新增「词→词改动」一档报告 | — |

`davenant_raw/colossians/manual_votes.json`：**157 条**按位置核定的条目
（键是 `卷\|扫描页号\|词形`）+ 11 条 `_` 开头的「不要改」备注。

### 五次差点改坏，都被回读或全书试跑拦下

| 改坏的 | 处数 | 根因 | 补的守卫 |
|---|---|---|---|
| `bad`→`had` | 21 | 短功能词规则缺「本来就是词」 | 加 `attested`；**不能**用「本书里 bad 出现 0 次」当判据——语料就是被改坏的产物，判据自我强化 |
| `member`→`memher`、`Mediator`→`Mediafor` | 12 | 断词跨行，后半 `ber,` 孤立看像 `her,`，`dehyph` 一接成假词 | 行首与带连字符的 token 不碰 |
| `Galat.`→`Gala†.`、`Rhet.`→`Rhe†.` | 8 | 词尾脚注符规则收了 `t` | 撤回——拉丁缩写多以 t 结尾 |
| `4to.`→`4to`、`No.`→`No`、`A.`→`A` | 5 | `_norm` 剥掉数字大小写，`4to.` 看着就是 `to.` | 判据改看原串 + 人名缩写/编号两道 |
| `NS`→`us`、`SOR`→`for`、`Hab.`→`Has.` | ~180 | 全大写碎片被当读花的功能词 | 全大写不碰 + 圣经/引书缩写表 |

**方法上最值钱的一条**：后两组是「把规则在全书上试跑一遍、把它想改的全部
列出来逐条看」才发现的，抽样看不出来。恢复后新加任何规则都要走这一步
（`scripts/davenant_dryrun.py`）。

另有一处判据问题：`web2` 只收词元，`withdrew` `interred` `bidden`
`qualifies` `dented` `proscribed` 查过去全是 False，于是这些**真词**被
`letter_repair` 列进候选。补了 `_stems()` 反向还原词干（不规则表 + 辅音重复
+ y→ies）。⚠️ 这条**只挂在 `letter_repair` 里，不能进全局 `attested`**：
`uppoint` 与 `loud` 也在 web2 里，进去会把 `uppointed`/`louded` 这两个
文件里记着的反例一起判成真词。

### 查出来「原书如此」，没改

- `Suares`（全书 14 次 vs `Suarez` 1 次）、`Auverne`（3 vs 1）
- `Levins` —— 书末勘误表自己写着「Page 325, Note, line 2, for Levins, read Lerins」
- `withcrafts`、分号冒号前的空格（19 世纪英式排法，全书 2070 处）

## 已知残留（都量化过，别当成没查）

1. **15 页、478 行编者长注落在正文里**，还被一行一段拆开
   （v1 p81/103/160/172/214/255/256/260/263/343/440/506/621、v2 p329/468）。
   成因与三次失败的补救尝试写在 `scripts/extract_davenant.py` 的 `split_page` 里。
   要修得先统一 `fn_max` 在管线内外的口径，再拿这 15 页当基准集。
2. 索引里判不出原文的：`Ape, Christ the head of`（首条条目词）、
   `Grabbon`、`Staphilus … 994`、`II. To Afilictions` 里那个被读花的页码。
3. `v2p355` 的 `put at from you`（应为 `put it`）——`at` 在同页出现 3 次，
   按页定位会落错处。
4. `v1p315` 的 `Wi. de`（原文 `Vide`）——W→V 已按位置修，但后半的 `de` 接不
   回去：`DOT_SPLIT` 有意不碰以 `de` 打头的后半（拉丁引文虚词靠它保住）。
5. 希腊文：互证不过的片段一律保留拉丁乱码，不采信。这是有意的。

## 验收口径

- `qa_davenant_regress.py`：逐词回退必须 **0 处**；「词→词」那一档要逐条核
  是不是人工核定的（规则本该只把非词修成词）。
- `qa_davenant.py` / `qa_davenant_appx.py`：结构闸 A–G。
- 渲染层：脚注配平、`<div>/<span>/<em>/<p>` 配对、无私用码残留、无重复锚点 id。

## 这一轮（302e17ef9）另外查出来的三件事

1. **管线把原文对的改错了**：原始 OCR 读的是 `Hom. 9. in Epist.`，
   「第二证人」规则拿 IA 那边的 `Horn.` 盖掉了它（引书缩写不在英文词典里，
   被判成「我方非词」）。同型 serm./Deut./contempl. 合计十余处。根因已堵。
   **查错次序**：原始 OCR 读的是什么 → 管线做了什么 → 才谈加规则。
2. **行级规则看不到跨行上下文**：`] Cor.` 的 `]` 在行末、`Cor.` 在下一行。
   补了段落级 `para_fix()` 与索引条目级 `fix_vol()`。
3. **人工票静默失效**：157 条里只有 90 条真落到实处（尾标点剥离不全、
   跨行断词的词只在 dehyph 后成形、页号系统性偏 1）。自检落成
   `scripts/davenant_vote_audit.py`，注意它的盲区（只走正文源）。

## 用户还在等的事

中文翻译仍**停着**（用户在本任务开始时说「中文先别动」）。`zh_cache` 里有
438 个已译块，zh 页面还建在旧英文上。英文这边已收尾，可以谈中文了。
